if we're at the bottom and hit still hit tab, query the database for the next 
page and if we get something back, we can reset that selector and render the new books. then update the page counter

header content -> slice of string
book item list content -> slice of string
book details -> string
footer message -> string

contents to put into items = []string
